{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pet",
  "description": "Minimal capability surface for the non-focusable Hara pet overlay",
  "windows": [
    "pet"
  ],
  "permissions": [
    "core:event:default",
    "core:window:default",
    "core:window:allow-set-focus",
    "core:window:allow-start-dragging"
  ]
}
